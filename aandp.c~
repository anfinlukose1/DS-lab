#include<stdio.h>
#include<stdlib.h>
void main()
{
int l,b,a,p;
printf("enter length and breath of the rectangle");
scanf("%d %d",&l,&b);
a=l*b;
p=2*(l+b);
printf("area and perimeter is:%d,%d",a,p);
}
